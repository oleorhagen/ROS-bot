r2d2:
    # Publish all joint states
    joint_state_controller:
        type: joint_state_controller/JointStateController
        publish_rate: 50
                 
    # Wheel motor controllers
    # joint_back_wheels_controller:
    #     type: velocity_controllers/JointGroupVelocityController
    #     joints:
    #         - joint_left_back_wheel
    #         - joint_right_back_wheel
    #     gains:
    #         - { p: 1.0, i: 1.0, d: 1.0 }
    #         - { p: 1.0, i: 1.0, d: 1.0 }

    ackermann_steering_controller:
        type: ackermann_steering_controller/AckermannSteeringController
        rear_wheel: 'joint_right_back_wheel'
        front_steer: 'joint_left_front_steer_bar'
        pose_covariance_diagonal: [0.001, 0.001, 1000000.0, 1000000.0, 1000000.0, 1000.0]
        twist_covariance_diagonal: [0.001, 0.001, 1000000.0, 1000000.0, 1000000.0, 1000.0]

        wheel_separation_h : 0.8 # TODO - Figure out this exactly (Should be pretty close)
        wheel_radius : 0.2

        # Wheel separation and radius multipliers for odometry calibration.
        wheel_separation_h_multiplier: 1.0 # default: 1.0
        wheel_radius_multiplier    : 1.0 # default: 1.0

        # Steer position angle multipliers for fine tuning.
        steer_pos_multiplier       : 1.0

        # Velocity commands timeout [s], default 0.5
        cmd_vel_timeout: 0.25

        # Base frame_id
        # base_frame_id: base_footprint #default: base_link
        base_frame_id: base_link

        # Odom frame_id
        odom_frame_id: odom

        # Velocity and acceleration limits
        # Whenever a min_* is unspecified, default to -max_*
        linear:
            x:
                has_velocity_limits    : true
                max_velocity           : 1.0  # m/s
                min_velocity           : -0.5 # m/s
                has_acceleration_limits: true
                max_acceleration       : 0.8  # m/s^2
                min_acceleration       : -0.4 # m/s^2
                has_jerk_limits        : true
                max_jerk               : 5.0 # m/s^3

        angular:
            z:
                has_velocity_limits    : true
                max_velocity           : 1.7  # rad/s
                has_acceleration_limits: true
                max_acceleration       : 1.5  # rad/s^2
                has_jerk_limits        : true
                max_jerk               : 2.5 # rad/s^3

    # joint_right_back_wheel_controller:
    #     type: velocity_controllers/JointVelocityController
    #     joint: joint_right_back_wheel
    #     pid: { p: 1000.0, i: 10.0, d: 1.0 }

    # # Front steering

    # joint_front_right_steer_bar_controller:
    #     type: effort_controllers/JointPositionController
    #     joint: joint_front_right_steer_bar
    #     pid: { p: 1.0, i: 0.1, d: 1.0 }

    # joint_front_left_steer_bar_controller:
    #     type: effort_controllers/JointPositionController
    #     joint: joint_front_left_steer_bar
    #     pid: { p: 1.0, i: 0.1, d: 1.0 }


    # gazebo_ros_control:   
    #     pid_gains:
    #         joint_left_back_wheel:
    #             p: 100000.0
    #             i: 10.0
    #             d: 0.50

    #         joint_right_back_wheel:
    #             p: 100000.0
    #             i: 10.0
    #             d: 0.50

    #         joint_front_steer_bar:
    #             p: 100000.0
    #             i: 10.0
    #             d: 0.50

    #         joint_left_front_steer_bar:
    #             p: 100000.0
    #             i: 10.0
    #             d: 0.50

    #         joint_right_front_steer_bar:
    #             p: 100000.0
    #             i: 10.0
    #             d: 0.50
